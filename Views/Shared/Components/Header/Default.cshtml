@model UserDto
<nav class="customNav">
    <h1>Chat en MVC</h1>


    @if (Model is not null)
    {
        <div class="userInfo">
            <p>Hola @Model.Nombre</p>
            <a asp-action="CerrarSesion" asp-controller="Home" asp-route-idUsuario="@Model.IdUsuario">Cerrar Sesion</a>
        </div>
    }
</nav>